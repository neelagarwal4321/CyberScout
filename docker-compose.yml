version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: cyberscout-app:latest
    container_name: cyberscout-app
    ports:
      - "3000:80"
    restart: unless-stopped
